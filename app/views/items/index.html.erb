<h2>アイテム一覧</h2>
<div>
  <% @items.each do |item| %>
    <li>
      <%= link_to item.name, item_path(item) %><br>
      <%= image_tag item.img.url %>
      <p>説明文</p>
      <p><%= item.subject %></p>
    </li>
  <% end %>
</div>